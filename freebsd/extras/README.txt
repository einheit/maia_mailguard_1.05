The freebsd-fixer script can be run after the FreeBSD Maia Mailguard port is installed, to take care of the final installation details.

The update-cfg-files script can be run as the last step to configure the settings for this installation.

